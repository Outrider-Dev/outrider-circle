// preload.js
// Kept intentionally minimal and secure — add IPC methods later if needed.
window.addEventListener('DOMContentLoaded', () => {
  // placeholder for future IPC hooks (auto-updates, file access)
});
